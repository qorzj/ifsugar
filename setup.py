#!/usr/bin/env python

from setuptools import setup

setup(name='ifsugar',
       version='0.0.1',
       description='syntax sugars for easyer understood coding: _if, _try, _get, _nil, _times',
       author='qorzj',
       author_email='inull@qq.com',
       maintainer='qorzj',
       maintainer_email='inull@qq.com',
       url='https://github.com/qorzj/ifsugar',
       keywords='ifsugar lambda syntax sugar times nil try',
       packages=['ifsugar'],
       long_description="syntax sugars for easyer understood coding: _if, _try, _get, _nil, _times",
       license="Public domain",
       platforms=["any"],
       install_requires=[],
      )

